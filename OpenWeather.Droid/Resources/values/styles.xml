<?xml version="1.0" encoding="utf-8" ?>
<resources>
  <!-- Base application theme. -->
  <style name="WeatherAppNoTitle" parent="android:Theme.Material.Light">
    <!-- Customize your theme here. -->
    <item name="@android:windowNoTitle">true</item>
    <item name="@android:colorPrimary">@color/colorPrimaryDark</item>
    <item name="@android:colorPrimaryDark">@color/colorPrimaryOrange</item>
    <item name="@android:colorAccent">@color/colorAccent</item>
    <item name="@android:textColorPrimary">@color/white</item>
  </style>

  <style name="WeatherAppTitle" parent="android:Theme.Material.Light">
    <!-- Customize your theme here. -->

    <item name="@android:colorPrimary">@color/colorPrimaryDark</item>
    <item name="@android:colorPrimaryDark">@color/colorPrimaryOrange</item>
    <item name="@android:colorAccent">@color/colorAccent</item>
    <item name="@android:textColorPrimary">@color/white</item>
  </style>
</resources>